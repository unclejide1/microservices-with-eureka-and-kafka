server:
  port: 8765
spring:
  application:
    name: ms-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: ms-config-server
ribbon:
  ReadTimeout: 60000
  SocketTimeout: 60000